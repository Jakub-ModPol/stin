const Discord = require("discord.js");
const fs = require("fs");

module.exports.run = async (bot, message, args) => {
    message.channel.send('Hmm, podívám se do kalendáře...')

}    
module.exports.help = {
    name: "cojedneszaden" 
}